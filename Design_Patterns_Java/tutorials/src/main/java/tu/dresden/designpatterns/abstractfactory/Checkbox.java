package tu.dresden.designpatterns.abstractfactory;


//Checkbox is the second product family. It has the same variants (behaviors) as buttons have.
public interface Checkbox {
	void doSomeAction(); 
}
